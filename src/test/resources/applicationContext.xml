<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:sws="http://www.springframework.org/schema/web-services"
       xmlns:task="http://www.springframework.org/schema/task" xmlns:cache="http://www.springframework.org/schema/cache"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
                        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.1.xsd
                        http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.1.xsd 
                        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd 
                        http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-4.1.xsd 
                        http://www.springframework.org/schema/web-services http://www.springframework.org/schema/web-services/web-services-4.1.xsd
                        http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.1.xsd
			http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd"
       default-autowire="byName">

    <!-- Read any *.properties file -->
    <context:property-placeholder location="classpath*:*.properties"/>

    <!-- Scan for beans -->
    <context:component-scan
            base-package="com.biit.activiti.groups, com.biit.activiti.users, com.biit.liferay.access, com.biit.activiti.security, com.biit.liferay.security"/>

    <bean
            class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator"/>

    <!-- Flowable Transaction Manager -->
    <bean id="flowableTransactionManager"
          class="org.springframework.orm.jpa.JpaTransactionManager"> <!--class="org.springframework.jdbc.datasource.DataSourceTransactionManager">-->
        <property name="entityManagerFactory" ref="flowableProjectManagerFactory"/>
        <property name="jpaDialect" ref="hibernateJpaDialect"/>
    </bean>

    <!-- Flowable Entity Manager -->
    <bean id="flowableProjectManagerFactory"
          class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="flowableDataSource"/>
        <property name="packagesToScan">
            <list>
                <value></value>
            </list>
        </property>
        <property name="jpaVendorAdapter" ref="hibernateVendor"/>
        <property name="jpaProperties" ref="flowableJpaProperties"/>
    </bean>

    <bean id="hibernateJpaDialect" class="org.springframework.orm.jpa.vendor.HibernateJpaDialect"/>

    <!-- Flowable Data Source -->
    <bean id="flowableDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource"
          destroy-method="close"> <!--class="org.springframework.jdbc.datasource.SimpleDriverDataSource">-->
        <property name="driverClass" value="${hibernate.connection.driver_class}"/>
        <property name="jdbcUrl" value="${hibernate.connection.url}"/>
        <property name="user" value="${hibernate.connection.username}"/>
        <property name="password" value="${hibernate.connection.password}"/>
        <!-- C3p0 -->
        <property name="minPoolSize" value="${c3p0.min_size}"/>
        <property name="maxPoolSize" value="${c3p0.max_size}"/>
        <property name="maxStatements" value="${c3p0.max_statements}"/>
        <property name="idleConnectionTestPeriod" value="${c3p0.idle_test_period}"/>
        <property name="loginTimeout" value="${c3p0.timeout}"/>
        <property name="acquireIncrement" value="${c3p0.acquire_increment}"/>
    </bean>

    <!-- Common JPA properties for USMO and Webforms -->
    <bean id="commonJpaProperties" parent="hibernateJpaProperties">
        <property name="sourceMap">
            <map merge="true"/>
        </property>
    </bean>

    <!-- Specific properties for Flowable -->
    <bean id="flowableJpaProperties" parent="commonJpaProperties">
        <property name="sourceMap">
            <map merge="true">
                <entry key="hibernate.dialect" value="${hibernate.dialect}"/>
                <entry key="hibernate.hbm2ddl.import_files" value="${hibernate.hbm2ddl.import_files}"/>
            </map>
        </property>
    </bean>

    <util:map id="hibernateJpaProperties">
        <entry key="hibernate.show_sql" value="${hibernate.show_sql}"/>
        <entry key="hibernate.use_sql_comments" value="${hibernate.use_sql_comments}"/>
        <entry key="hibernate.format_sql" value="${hibernate.format_sql}"/>
        <entry key="hibernate.hbm2ddl.auto" value="${hibernate.hbm2ddl.auto}"/>
        <!-- New ID creator -->
        <entry key="hibernate.id.new_generator_mappings" value="${hibernate.id.new_generator_mappings}"/>
        <!-- Unnecessary to open and close sessions -->
        <entry key="hibernate.current_session_context_class" value="${hibernate.current_session_context_class}"/>
        <!-- When deleting an object, set ID to null -->
        <entry key="hibernate.use_identifier_rollback" value="${hibernate.use_identifier_rollback}"/>
        <entry key="hibernate.order_inserts" value="${hibernate.order_inserts}"/>
        <entry key="hibernate.order_updates" value="${hibernate.order_updates}"/>
        <entry key="hibernate.jdbc.batch_versioned_data" value="${hibernate.jdbc.batch_versioned_data}"/>
        <entry key="hibernate.jdbc.use_streams_for_binary" value="${hibernate.jdbc.use_streams_for_binary}"/>
        <entry key="hibernate.max_fetch_depth" value="${hibernate.max_fetch_depth}"/>
        <!-- Second Level Cache -->
        <entry key="hibernate.cache.use_second_level_cache" value="${hibernate.cache.use_second_level_cache}"/>
        <entry key="hibernate.cache.region.factory_class" value="${hibernate.cache.region.factory_class}"/>
        <entry key="hibernate.cache.use_query_cache" value="${hibernate.cache.use_query_cache}"/>
        <entry key="hibernate.cache.provider_class" value="${hibernate.cache.provider_class}"/>
        <entry key="hibernate.cache.use_structured_entries" value="${hibernate.cache.use_structured_entries}"/>
        <entry key="hibernate.generate_statistics" value="${hibernate.generate_statistics}"/>
        <entry key="hibernate.cache.provider_configuration_file_resource_path"
               value="${hibernate.cache.provider_configuration_file_resource_path}"/>
        <entry key="hibernate.cache.provider_class" value="${hibernate.cache.provider_class}"/>
        <entry key="hibernate.cache.use_query_cache" value="${hibernate.cache.use_query_cache}"/>
    </util:map>

    <bean id="hibernateVendor"
          class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
    </bean>

    <!-- Flowable Database Access -->
    <bean id="processEngine" class="org.flowable.spring.ProcessEngineFactoryBean"
          destroy-method="destroy">
        <property name="processEngineConfiguration" ref="processEngineConfiguration"/>
    </bean>

    <bean id="processEngineConfiguration" class="org.flowable.spring.SpringProcessEngineConfiguration">
        <property name="transactionManager" ref="flowableTransactionManager"/>
        <property name="dataSource" ref="flowableDataSource"/>
        <property name="databaseType" value="mysql"/>
        <!--<property name="jobExecutorActivate" value="false" />-->
        <property name="databaseSchemaUpdate" value="${hibernate.hbm2ddl.auto}"/>
        <!-- <property name="history" value="full" /> -->
        <property name="customSessionFactories">
            <list>
                <bean class="com.biit.activiti.users.ActivitiUserManagerFactory"/>
                <bean class="com.biit.activiti.groups.ActivitiGroupManagerFactory"/>
            </list>
        </property>
        <property name="deploymentResources" value="classpath*:/process/*.bpmn20.xml"/>
        <property name="mailServerHost" value="smtp.1and1.es"/>
        <property name="mailServerPort" value="25"/>
        <property name="mailServerDefaultFrom" value="noreply@biit-solutions.com"/>
        <property name="mailServerUsername" value="noreply@biit-solutions.com"/>
        <property name="mailServerPassword" value="N0r3pl@2048"/>
    </bean>

    <bean id="flowableRule" class="org.flowable.engine.test.FlowableRule">
        <property name="processEngine" ref="processEngine"/>
    </bean>

</beans>